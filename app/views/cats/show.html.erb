<h1 class="text-center my-3"><%= @cat.name %></h1>

<div class="container w-50 mx-auto">
  <div class="card">
    <div class="card-body">
      <h5 class="card-title"><%= @cat.name %></h5>
      <p class="card-text"><%= @cat.cat_breed.name %></p>
      <p class="card-text"><%= @cat.age %> years old</p>
    </div>
  </div>
</div>